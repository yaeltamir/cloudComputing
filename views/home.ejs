<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>SugarSnap</title>
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="assets/logo3.png" />
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
    <!-- Bootstrap and custom styles -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <link href="css/styles.css" rel="stylesheet" />

</head>

<body class="d-flex flex-column h-100">
    <main class="flex-shrink-0">
        <!-- Navigation bar -->
        <nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
            <div class="container px-5">
                <a class="navbar-brand" href="index"><span class="fw-bolder text-primary">Home</span></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small fw-bolder">
                        <!-- Navigation links -->
                        <li class="nav-item"><a class="nav-link" href="home">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="meals">Meals</a></li>
                        <li class="nav-item"><a class="nav-link" href="historyGraph">History</a></li>
                        <li class="nav-item"><a class="nav-link" href="updateDetails">Update</a></li>
                        <li class="nav-item"><a class="nav-link" href="messages">Messages</a></li>
                    </ul>
                </div>
            </div>
        </nav>


        <!-- Welcome section -->
        <div class="text-center mb-5">
            <h1 class="display-3 fw-bolder mb-5 mt-4"><span class="text-gradient d-inline">Welcome Back, <%= userName %>!</span></h1>
             <!-- Notification for non-registered users -->
             <% if (!isRegistered) { %>
                <div class="notification-container">
                    <div class="notification-message">
                        <p>You are not registered for our messaging services. Please subscribe to stay updated!</p>
                    </div>
                </div>
            <% } %>
            <h1 class="display-5 fw-bolder mb-0 mt-4">
                <span class="text-gradient d-inline">Your last meals:</span>
            </h1>
        </div>

        <!-- Last meals section -->
        <div class="last-meals-container">
            <div class="meal-container">
                <% if (lastThreeMeals && lastThreeMeals.length > 0) { %>
                    <% lastThreeMeals.forEach(function(meal) { %>
                        <div class="meal-item">
                            <!-- Meal image -->
                            <img src="<%= meal.imageUrl %>" alt="<%= meal.kindOfMeal %>" />
                            <!-- Meal description -->
                            <div class="meal-description">
                                <h5><%= meal.kindOfMeal.charAt(0).toUpperCase() + meal.kindOfMeal.slice(1) %></h5>
                                <p><%= new Date(meal.Date).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) %></p>
                                <p><%= new Date(meal.Time).toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit' }) %></p>
                                <p>Sugar Level: <%= meal.sugarLevel %></p>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>No meals found!</p>
                <% } %>
            </div>
        </div>

        <!-- Footer section -->
        <section class="bg-light py-5">
            <div class="container px-5">
                <div class="row gx-5 justify-content-center">
                    <div class="col-xxl-8">
                        <div class="text-center my-5">
                            <h2 class="display-5 fw-bolder"><span class="text-gradient d-inline"> ♥ ♥ ♥ ♥ ♥</span></h2>
                            <p class="lead fw-light mb-4">All rights reserved. | Designed with ♥</p>
                            <p class="text-muted">Copyright &copy; SugarSnap 2024</p>
                            <div class="d-flex justify-content-center fs-2 gap-4">
                                <a class="text-gradient" href="#!"><i class="bi bi-twitter"></i></a>
                                <a class="text-gradient" href="#!"><i class="bi bi-linkedin"></i></a>
                                <a class="text-gradient" href="#!"><i class="bi bi-github"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- Include message notification partial -->
    <%- include('partials/messageNotification') %>
</body>
</html>
